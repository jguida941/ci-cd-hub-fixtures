# Python Failing Fixture Dependencies
# Some packages here have KNOWN VULNERABILITIES for testing pip-audit/Trivy

# Testing
pytest>=7.4.0
pytest-cov>=4.1.0
hypothesis>=6.92.0

# Type checking
mypy>=1.8.0

# Linting/formatting (for local dev)
ruff>=0.1.9
black>=23.12.0
isort>=5.13.0

# Security scanning (for local dev)
bandit>=1.7.7
pip-audit>=2.6.1

# Mutation testing
mutmut>=3.0.0

# ============================================================
# INTENTIONALLY VULNERABLE DEPENDENCIES FOR TESTING
# ============================================================
# urllib3 1.26.4 - CVE-2021-33503 (DoS via regex)
urllib3==1.26.4

# Pillow 8.2.0 - Multiple CVEs (buffer overflow, DoS)
Pillow==8.2.0

# requests 2.25.0 - CVE-2023-32681 (proxy auth leak)
requests==2.25.0

# PyYAML 5.3 - CVE-2020-14343 (arbitrary code execution)
PyYAML==5.3

# Note: The code in this fixture has intentional security issues
# that Bandit and Semgrep will detect (hardcoded passwords, eval, etc.)
